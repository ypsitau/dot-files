(define-derived-mode gura-mode prog-mode "Gura"
  "Major mode for editing Gura programming language."
  ;;(set-syntax-table gura-mode-syntax-table)
  ;;(setq-local font-lock-defaults '(gura-mode-font-lock-keywords nil nil))
  )

(provide 'gura-mode)
